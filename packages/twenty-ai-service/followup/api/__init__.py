"""Follow-Up Agent REST API (Step 7).

Exposes the review / accept / reject / revise loop and the follow-up trigger
endpoints. Mounted via ``app.include_router(followup.api.routes.router)``
in ``main.py``.
"""
