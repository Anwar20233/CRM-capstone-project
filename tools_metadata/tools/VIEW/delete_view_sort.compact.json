{
  "name": "delete_view_sort",
  "description": "Remove a sort from a view. Use get_view_sorts to find the sort ID.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "description": "ID of the view sort to delete",
        "type": "string",
        "format": "uuid"
      }
    },
    "required": [
      "id"
    ]
  }
}