{
  "name": "delete_view",
  "description": "Delete a view by its ID.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "description": "View ID to delete",
        "type": "string",
        "format": "uuid"
      }
    },
    "required": [
      "id"
    ]
  }
}